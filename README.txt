Run frontend by:

npx create-react-app
#replace the public, source and json files with the one given below
npm start

Run Backend by:

#replace all files with the given backend files
cd backend
nodemon index.js


import product file in mongodb database